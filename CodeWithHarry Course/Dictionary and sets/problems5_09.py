# 9. Can you change the values inside a list which is contained in set S?
# s = {8, 7, 12, "Harry", [1,2]}

# No, you cannot change the values inside a list contained in a set because sets only allow
# immutable elements,
# and lists are mutable and therefore not allowed in sets.